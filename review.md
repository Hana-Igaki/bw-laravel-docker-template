# Laravel Lesson レビュー①

## Todo一覧機能

### Todoモデルのallメソッドで実行しているSQLは何か
- `SELECT * FROM todos;`

### Todoモデルのallメソッドの返り値は何か
- Illuminate\Database\Eloquent\Collection

### 配列の代わりにCollectionクラスを使用するメリットは
- ファイル管理の可視化
- セキュリティーの向上

### view関数の第1・第2引数の指定と何をしているか
- 第一引数：表示したいファイル
- 第二引数：使いたい変数

### index.blade.phpの$todos・$todoに代入されているものは何か
- `$todos`：todoテーブルの全レコード
- `$todo`：todoテーブルの各レコード

## Todo作成機能

### Requestクラスのallメソッドは何をしているか
- 入力された全ての値を取得している

### fillメソッドは何をしているか
- 入力されたデータのキーと値を取得している

### $fillableは何のために設定しているか
- 一括代入の脆弱性を補うため

### saveメソッドで実行しているSQLは何か
- `INSERT INTO テーブル名 (列名1, 列名2,...) VALUES (値1, 値2,...);`

### redirect()->route()は何をしているか
- 処理が終わった後に`route()`内のページに遷移

## その他

### テーブル構成をマイグレーションファイルで管理するメリット
- PHPを知らなくてもテーブル操作ができる
- 開発者全員が同じ環境を共有できる

### マイグレーションファイルのup()、down()は何のコマンドを実行した時に呼び出されるのか
- up()：`artisan`
- down()：`rollback`

### Seederクラスの役割は何か
- レコードの作成

### route関数の引数・返り値・使用するメリット
- 引数：遷移したいファイル
- 返り値：遷移先のURL
- メリット：URLが変わってもファイルが変わらなければ修正箇所を少なく抑えられる

### @extends・@section・@yieldの関係性とbladeを分割するメリット
- @extends：継承する親Bladeを指定
- @section：親Bladeの@yieldに@sectionで囲われている部分を挿入
- Bladeを分割するメリット：複数のページの共通部分を一つのファイルでまとめることができるので、記述量が少なく済む

### @csrfは何のための記述か
- CSRF対策

### {{ }}とは何の省略系か
- `<?php echo ?>`